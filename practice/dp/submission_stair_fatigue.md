# 피로도를 고려한 계단 학습 - 제출물

## 문제

1번부터 n번까지 계단이 있고, `fatigue[i]`는 i+1번 계단에 착지할 때 드는 피로도.
지면에서 시작해 한 번에 1칸 또는 2칸씩 올라 마지막 계단에 도착.
**2칸 점프를 연속으로 두 번 쓸 수 없음.** 착지 피로도 합의 최솟값을 반환.

```
fatigue = [4, 2, 7, 1, 3] -> answer = 10  (1 -> 2 -> 4 -> 5에 착지: 4+2+1+3)
fatigue = [9, 1]          -> answer = 1   (지면에서 2번 계단으로 한 번 점프)
```

## 1. dp 상태의 의미

`dp[i][0]` = (여기에 작성)

`dp[i][1]` = (여기에 작성)

## 2. 초기값

`dp[0][?]` = (여기에 작성) — 이유:

`dp[1][0]` = (여기에 작성) — 이유:

`dp[1][1]` = (여기에 작성) — 이유:

## 3. 전이식

`dp[i][0] = ` (여기에 작성)

- 이유:

`dp[i][1] = ` (여기에 작성)

- 이유:

## 4. n=1, n=2 직접 추적

### n=1 (예: fatigue=[7])

(여기에 dp 테이블 값을 손으로 채워서 작성)

### n=2 (예: fatigue=[9,1])

(여기에 dp 테이블 값을 손으로 채워서 작성)

## 5. 시간/공간 복잡도

시간복잡도: (여기에 작성)

공간복잡도: (여기에 작성)

## 최종 코드

```python
def minimum_stair_fatigue(fatigue):
    n -=
```
