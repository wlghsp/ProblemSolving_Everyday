# LeetCode 443. String Compression - 힌트

## 문제 요약
문자 배열을 **제자리(in-place)에서 압축**하는 문제입니다.
- 연속된 같은 문자를 `문자 + 개수`로 압축
- 개수가 1이면 숫자 생략
- 압축된 길이를 반환

### 예시
```
입력: ["a","a","b","b","c","c","c"]
출력: 6
배열: ["a","2","b","2","c","3"]

입력: ["a"]
출력: 1
배열: ["a"]

입력: ["a","b","b","b","b","b","b","b","b","b","b","b","b"]
출력: 4
배열: ["a","b","1","2"]  (b가 12개 → "12"는 두 글자)
```

### 제약 조건
- 1 ≤ chars.length ≤ 2000
- 영문 대소문자, 숫자, 특수문자
- **반드시 O(1) 추가 공간** (in-place)

---

## Level 1 - 방향 힌트

**Two Pointer** 문제입니다.
- read 포인터: 원본 배열을 읽는 위치
- write 포인터: 압축 결과를 쓰는 위치
- 같은 문자가 연속으로 몇 개인지 세고, write 위치에 기록

---

## Level 2 - 구조 힌트

```python
def compress(self, chars):
    # write: 결과를 쓸 위치
    # read: 원본을 읽을 위치

    # read가 끝까지 갈 때까지
        # 현재 문자 기억
        # 같은 문자가 몇 개 연속인지 세기 (count)

        # write 위치에 문자 쓰기
        # count가 1보다 크면 숫자도 쓰기
        #   주의: 12 같은 여러 자리 숫자는 '1', '2'로 나눠 쓰기

    # write 반환 (압축된 길이)
```

---

## Level 3 - 전체 풀이

```python
def compress(self, chars: List[str]) -> int:
    write = 0  # 결과를 쓸 위치
    read = 0   # 원본을 읽을 위치

    while read < len(chars):
        current_char = chars[read]
        count = 0

        # 같은 문자가 연속으로 몇 개인지 세기
        while read < len(chars) and chars[read] == current_char:
            read += 1
            count += 1

        # 문자 쓰기
        chars[write] = current_char
        write += 1

        # 개수가 2 이상이면 숫자도 쓰기
        if count > 1:
            # 숫자를 문자열로 변환 후 한 글자씩 쓰기
            # 예: 12 → '1', '2'
            for digit in str(count):
                chars[write] = digit
                write += 1

    return write
```

### 동작 예시: ["a","a","b","b","c","c","c"]

```
read=0: 'a' 연속 2개 → chars[0]='a', chars[1]='2' → write=2
read=2: 'b' 연속 2개 → chars[2]='b', chars[3]='2' → write=4
read=4: 'c' 연속 3개 → chars[4]='c', chars[5]='3' → write=6

결과: ["a","2","b","2","c","3"], 반환: 6
```

---

## 🤔 자주하는 실수

### 1. 개수 1일 때 숫자를 쓰는 실수
```python
# ❌ 개수가 1이어도 숫자를 씀
# ["a"] → ["a", "1"] (틀림)

# ✅ 개수가 2 이상일 때만
if count > 1:
    for digit in str(count):
        chars[write] = digit
        write += 1
```

### 2. 여러 자리 숫자 처리 안 함
```python
# ❌ 숫자를 한 글자로 쓰려고 함
chars[write] = str(count)  # count=12이면 "12" 한 칸에? 안 됨

# ✅ 한 자리씩 분리해서 쓰기
for digit in str(count):  # "12" → '1', '2'
    chars[write] = digit
    write += 1
```

---

## 🎯 시간/공간 복잡도

- **시간: O(n)** - 배열 한 번 순회
- **공간: O(1)** - in-place 수정 (추가 공간 없음)

---

## 🚀 관련 문제

- LeetCode 38: Count and Say
- LeetCode 394: Decode String (압축 해제)
- LeetCode 271: Encode and Decode Strings

화이팅! Two Pointer로 in-place 처리하는 패턴은 자주 나옵니다! 🔥
