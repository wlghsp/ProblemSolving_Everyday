# LeetCode 151. Reverse Words in a String - 힌트

## 문제 요약
문자열에서 **단어의 순서를 뒤집는** 문제입니다.
- 앞뒤 공백 제거
- 단어 사이 공백은 1개로 통일
- 단어 순서 뒤집기

### 예시
```
입력: "the sky is blue"
출력: "blue is sky the"

입력: "  hello world  "
출력: "world hello"

입력: "a good   example"
출력: "example good a"
```

### 제약 조건
- 1 ≤ s.length ≤ 10^4
- 영문자, 숫자, 공백으로만 구성
- 최소 1개의 단어 존재

---

## 💡 핵심 아이디어

### 방법 1: Python 내장 함수 활용 (가장 간단) ⭐

```python
def reverseWords(self, s: str) -> str:
    # 1. split(): 공백 기준 분리 (연속 공백 자동 처리)
    # 2. reversed(): 순서 뒤집기
    # 3. join(): 공백으로 연결
    return ' '.join(s.split()[::-1])
```

**또는:**
```python
def reverseWords(self, s: str) -> str:
    words = s.split()      # ['the', 'sky', 'is', 'blue']
    words.reverse()        # ['blue', 'is', 'sky', 'the']
    return ' '.join(words) # "blue is sky the"
```

**split()의 마법:**
- `"  hello world  ".split()` → `['hello', 'world']`
- 앞뒤 공백, 연속 공백 모두 자동 처리!

---

### 방법 2: Two Pointer (면접용)

**O(1) 추가 공간을 요구할 때:**

```python
def reverseWords(self, s: str) -> str:
    # 문자열을 리스트로 변환 (Python 문자열은 immutable)
    chars = list(s)

    # 1. 전체 뒤집기
    self.reverse(chars, 0, len(chars) - 1)

    # 2. 각 단어 뒤집기
    self.reverseEachWord(chars)

    # 3. 공백 정리
    return self.cleanSpaces(chars)

def reverse(self, chars, left, right):
    while left < right:
        chars[left], chars[right] = chars[right], chars[left]
        left += 1
        right -= 1
```

**동작 예시:**
```
원본: "the sky is blue"
1단계 (전체 뒤집기): "eulb si yks eht"
2단계 (단어별 뒤집기): "blue is sky the"
```

---

### 방법 3: 스택 활용

```python
def reverseWords(self, s: str) -> str:
    stack = []
    word = ""

    for char in s:
        if char != ' ':
            word += char
        elif word:
            stack.append(word)
            word = ""

    if word:  # 마지막 단어
        stack.append(word)

    result = []
    while stack:
        result.append(stack.pop())

    return ' '.join(result)
```

---

## 🎯 방법별 비교

| 방법 | 시간 | 공간 | 특징 |
|------|------|------|------|
| **split + join** | O(n) | O(n) | 가장 간결, 파이썬 추천 |
| **Two Pointer** | O(n) | O(n)* | 면접용, 공간 최적화 |
| **스택** | O(n) | O(n) | 직관적 |

*Python은 문자열이 immutable이라 O(n) 필요, C++/Java는 O(1) 가능

---

## 🤔 자주하는 실수

### 1. split() 사용법 혼동
```python
# ❌ split(' ') - 빈 문자열 포함
"  hello  world  ".split(' ')  # ['', '', 'hello', '', 'world', '', '']

# ✅ split() - 연속 공백 자동 처리
"  hello  world  ".split()  # ['hello', 'world']
```

### 2. 뒤집기 방법 혼동
```python
# 리스트 뒤집기
words[::-1]       # 새 리스트 반환
words.reverse()   # 제자리 뒤집기 (None 반환)
reversed(words)   # iterator 반환

# ❌ 잘못된 사용
return ' '.join(words.reverse())  # None을 join하려 함

# ✅ 올바른 사용
words.reverse()
return ' '.join(words)
# 또는
return ' '.join(words[::-1])
```

---

## 💪 핵심 포인트 체크리스트

- [ ] `split()`이 연속 공백을 자동 처리하는 것을 아나요?
- [ ] 리스트 뒤집기 방법 3가지를 아나요?
- [ ] `join()`으로 리스트를 문자열로 합치는 법을 아나요?
- [ ] Two Pointer 방식의 원리를 이해했나요?

---

## 🎯 시간/공간 복잡도

**split + join 방식:**
- 시간: O(n) - 문자열 한 번 순회
- 공간: O(n) - 단어 리스트 저장

---

## 🚀 추가 도전 과제

1. **Follow-up: O(1) 추가 공간으로 풀기**
   - Python에서는 문자열이 immutable이라 불가능
   - C++/Java에서는 가능 (문자 배열 직접 조작)

2. **관련 문제:**
   - LeetCode 186: Reverse Words in a String II (배열 버전)
   - LeetCode 557: Reverse Words in a String III (단어 내 문자 뒤집기)
   - LeetCode 344: Reverse String (문자열 뒤집기)

---

## 💡 한 줄 정리

**Python 풀이의 핵심:**
```python
return ' '.join(s.split()[::-1])
```

화이팅! 문자열 처리는 면접에서 자주 나오는 주제입니다! 🔥
