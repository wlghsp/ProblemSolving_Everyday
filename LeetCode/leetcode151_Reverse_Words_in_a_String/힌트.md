# LeetCode 151. Reverse Words in a String - 힌트

## 문제 요약
문자열에서 **단어의 순서를 뒤집는** 문제입니다.
- 앞뒤 공백 제거
- 단어 사이 공백은 1개로 통일
- 단어 순서 뒤집기

### 예시
```
입력: "the sky is blue"
출력: "blue is sky the"

입력: "  hello world  "
출력: "world hello"

입력: "a good   example"
출력: "example good a"
```

### 제약 조건
- 1 ≤ s.length ≤ 10^4
- 영문자, 숫자, 공백으로만 구성
- 최소 1개의 단어 존재

---

## Level 1 - 방향 힌트

**문자열 분리 + 뒤집기 + 합치기** 문제입니다.
- Python의 `split()`은 연속 공백을 자동으로 처리해줌
- 분리 → 뒤집기 → join으로 한 줄에 가능

---

## Level 2 - 구조 힌트

```python
def reverseWords(self, s: str) -> str:
    # 1. 공백 기준으로 단어 분리 (연속 공백 자동 처리)

    # 2. 단어 순서 뒤집기

    # 3. 공백 1개로 합치기
```

---

## Level 3 - 전체 풀이

### 방법 1: split + join (Python 추천) ⭐

```python
def reverseWords(self, s: str) -> str:
    # split(): 공백 기준 분리 (앞뒤/연속 공백 자동 처리)
    # [::-1]: 순서 뒤집기
    # join(): 공백 1개로 연결
    return ' '.join(s.split()[::-1])
```

**split()의 마법:**
```python
"  hello  world  ".split()   # ['hello', 'world']
"  hello  world  ".split(' ')  # ['', '', 'hello', '', 'world', '', '']
# 인자 없이 split()을 쓰면 연속 공백을 자동 처리!
```

### 방법 2: Two Pointer (면접용)

```python
def reverseWords(self, s: str) -> str:
    # Python 문자열은 immutable이라 리스트로 변환
    chars = list(s)

    def reverse(left, right):
        while left < right:
            chars[left], chars[right] = chars[right], chars[left]
            left += 1
            right -= 1

    # 1. 전체 뒤집기
    # "the sky" → "yks eht"
    reverse(0, len(chars) - 1)

    # 2. 각 단어 뒤집기
    # "yks eht" → "sky the"
    n = len(chars)
    start = 0
    for end in range(n + 1):
        if end == n or chars[end] == ' ':
            reverse(start, end - 1)
            start = end + 1

    # 3. 공백 정리 (앞뒤/연속 공백 제거)
    result = []
    i = 0
    while i < n:
        if chars[i] != ' ':
            if result and result[-1] != ' ':
                pass
            elif result:
                result.append(' ')
            while i < n and chars[i] != ' ':
                result.append(chars[i])
                i += 1
        else:
            i += 1

    return ''.join(result)
```

---

## 🤔 자주하는 실수

### 1. split()과 split(' ')의 차이
```python
# ❌ split(' ') - 빈 문자열 포함
"  hello  world  ".split(' ')  # ['', '', 'hello', '', 'world', '', '']

# ✅ split() - 연속 공백 자동 처리
"  hello  world  ".split()  # ['hello', 'world']
```

### 2. reverse()의 반환값
```python
# ❌ reverse()는 None을 반환
return ' '.join(words.reverse())  # TypeError!

# ✅ 두 가지 방법
words.reverse()           # 제자리 수정 후
return ' '.join(words)    # 따로 반환

return ' '.join(words[::-1])  # 또는 슬라이싱
```

---

## 🎯 시간/공간 복잡도

- **시간: O(n)** - 문자열 한 번 순회
- **공간: O(n)** - 단어 리스트 저장

---

## 🚀 관련 문제

- LeetCode 186: Reverse Words in a String II (배열 버전)
- LeetCode 557: Reverse Words in a String III (단어 내 문자 뒤집기)
- LeetCode 344: Reverse String (문자열 뒤집기)

화이팅! 🔥
