# LeetCode 394. Decode String - 힌트

## 문제 요약
인코딩된 문자열을 **디코딩**하는 문제입니다.
- `k[encoded_string]` → encoded_string을 k번 반복

### 예시
```
입력: "3[a]2[bc]"
출력: "aaabcbc"

입력: "3[a2[c]]"
출력: "accaccacc"
(안쪽부터: 2[c] → "cc" → a + cc = "acc" → 3번 = "accaccacc")

입력: "2[abc]3[cd]ef"
출력: "abcabccdcdcdef"
```

### 제약 조건
- 1 ≤ s.length ≤ 30
- 숫자는 1~300 범위
- 입력은 항상 유효함

---

## Level 1 - 방향 힌트

**스택(Stack)** 문제입니다.
- `[`를 만나면 현재 상태를 스택에 저장
- `]`를 만나면 스택에서 꺼내서 조합
- 괄호가 중첩될 수 있으므로 스택이 핵심

---

## Level 2 - 구조 힌트

```python
def decodeString(self, s: str) -> str:
    # 현재 문자열과 현재 숫자를 추적
    # 스택: 이전 상태를 저장하는 용도

    # 문자열을 한 글자씩 순회
        # 숫자면? 숫자 누적 (여러 자리 수 가능: "100")
        # '['면? 현재 상태를 스택에 push, 초기화
        # ']'면? 스택에서 pop, 반복 횟수만큼 곱해서 조합
        # 문자면? 현재 문자열에 추가

    # 결과 반환
```

---

## Level 3 - 전체 풀이

```python
def decodeString(self, s: str) -> str:
    stack = []
    current_str = ""   # 현재까지 만들어진 문자열
    current_num = 0    # 현재 숫자

    for char in s:
        if char.isdigit():
            # 숫자가 여러 자리일 수 있음: "12[a]" → 12
            current_num = current_num * 10 + int(char)

        elif char == '[':
            # 현재 상태를 스택에 저장하고 초기화
            # (이전 문자열, 반복 횟수)를 함께 저장
            stack.append((current_str, current_num))
            current_str = ""
            current_num = 0

        elif char == ']':
            # 스택에서 이전 상태 꺼내기
            prev_str, num = stack.pop()
            # 이전 문자열 + (현재 문자열 × 반복 횟수)
            current_str = prev_str + current_str * num

        else:
            # 일반 문자는 현재 문자열에 추가
            current_str += char

    return current_str
```

### 동작 예시: "3[a2[c]]"

```
'3' → current_num = 3
'[' → stack: [("", 3)], current_str = "", current_num = 0
'a' → current_str = "a"
'2' → current_num = 2
'[' → stack: [("", 3), ("a", 2)], current_str = "", current_num = 0
'c' → current_str = "c"
']' → pop ("a", 2) → current_str = "a" + "c"*2 = "acc"
']' → pop ("", 3) → current_str = "" + "acc"*3 = "accaccacc"
```

---

## 🤔 자주하는 실수

### 1. 여러 자리 숫자 처리 안 함
```python
# ❌ 한 자리만 처리
current_num = int(char)  # "100"이면 0만 저장됨

# ✅ 여러 자리 처리
current_num = current_num * 10 + int(char)  # 1 → 10 → 100
```

### 2. 스택에 저장할 때 초기화 안 함
```python
# ❌ push 후 초기화 안 하면 이전 값이 섞임
stack.append((current_str, current_num))

# ✅ push 후 반드시 초기화
stack.append((current_str, current_num))
current_str = ""
current_num = 0
```

---

## 🎯 시간/공간 복잡도

- **시간: O(n)** - 문자열 한 번 순회 (n = 디코딩된 결과 길이)
- **공간: O(n)** - 스택 + 결과 문자열

---

## 🚀 관련 문제

- LeetCode 20: Valid Parentheses (스택 기초)
- LeetCode 150: Evaluate Reverse Polish Notation (스택 응용)
- LeetCode 726: Number of Atoms (유사 패턴)

화이팅! 스택으로 괄호 처리하는 패턴을 익히면 많은 문제에 응용됩니다! 🔥
