# LeetCode 15. 3Sum - 힌트

## 문제 요약
배열에서 **합이 0이 되는 세 수의 조합**을 모두 찾는 문제입니다.
- 중복 조합 불가
- 같은 인덱스 중복 사용 불가

### 예시
```
입력: [-1, 0, 1, 2, -1, -4]
출력: [[-1, -1, 2], [-1, 0, 1]]
```

```
입력: [0, 1, 1]
출력: [] (합이 0인 조합 없음)

입력: [0, 0, 0]
출력: [[0, 0, 0]]
```

### 제약 조건
- 3 ≤ nums.length ≤ 3000
- -10^5 ≤ nums[i] ≤ 10^5

---

## 💡 핵심 아이디어

### 정렬 + Two Pointer ⭐

**3개를 한번에 찾지 말고, 1개를 고정하고 나머지 2개를 Two Pointer로!**

```
정렬: [-4, -1, -1, 0, 1, 2]

i 고정 → left, right로 나머지 2개 탐색

i=-4: left=-1, right=2 → 합=-3 (부족, left++)
i=-1: left=-1, right=2 → 합=0 ✓ → [-1, -1, 2]
      left=0, right=1  → 합=0 ✓ → [-1, 0, 1]
```

**왜 정렬이 필요한가?**
1. Two Pointer를 쓰려면 정렬 필수
2. 중복 제거가 쉬워짐 (같은 값 건너뛰기)

---

## 🔑 해결 전략

### 전체 코드

```python
def threeSum(self, nums: List[int]) -> List[List[int]]:
    nums.sort()  # 1. 정렬
    result = []
    n = len(nums)

    for i in range(n - 2):
        # 중복된 i 값 스킵
        if i > 0 and nums[i] == nums[i - 1]:
            continue

        # 2. Two Pointer
        left, right = i + 1, n - 1

        while left < right:
            total = nums[i] + nums[left] + nums[right]

            if total == 0:
                result.append([nums[i], nums[left], nums[right]])
                left += 1
                right -= 1

                # 3. 중복 제거
                while left < right and nums[left] == nums[left - 1]:
                    left += 1
                while left < right and nums[right] == nums[right + 1]:
                    right -= 1

            elif total < 0:
                left += 1    # 합이 작으면 left 증가
            else:
                right -= 1   # 합이 크면 right 감소

    return result
```

---

## 🎯 단계별 동작 예시

**입력: [-1, 0, 1, 2, -1, -4]**

```
정렬 후: [-4, -1, -1, 0, 1, 2]

i=0 (nums[i]=-4):
  left=1, right=5: -4 + (-1) + 2 = -3 < 0 → left++
  left=2, right=5: -4 + (-1) + 2 = -3 < 0 → left++
  left=3, right=5: -4 + 0 + 2 = -2 < 0 → left++
  left=4, right=5: -4 + 1 + 2 = -1 < 0 → left++
  left >= right → 끝

i=1 (nums[i]=-1):
  left=2, right=5: -1 + (-1) + 2 = 0 ✓ → [-1, -1, 2]
  left=3, right=4: -1 + 0 + 1 = 0 ✓ → [-1, 0, 1]
  left >= right → 끝

i=2 (nums[i]=-1): nums[2] == nums[1] → 스킵!

i=3 (nums[i]=0):
  left=4, right=5: 0 + 1 + 2 = 3 > 0 → right--
  left >= right → 끝

결과: [[-1, -1, 2], [-1, 0, 1]]
```

---

## 🤔 자주하는 실수

### 1. 중복 제거 안 함
```python
# ❌ 중복 조합 발생
# [-1, -1, 0, 1, 2]에서 [-1, 0, 1]이 두 번 나옴

# ✅ i 중복 스킵
if i > 0 and nums[i] == nums[i - 1]:
    continue

# ✅ left, right 중복 스킵
while left < right and nums[left] == nums[left - 1]:
    left += 1
while left < right and nums[right] == nums[right + 1]:
    right -= 1
```

### 2. i 중복 스킵 조건 실수
```python
# ❌ i > 0 체크 없이 하면 인덱스 에러
if nums[i] == nums[i - 1]:  # i=0일 때 nums[-1] 접근!

# ✅ i > 0 조건 추가
if i > 0 and nums[i] == nums[i - 1]:
    continue
```

### 3. total == 0일 때 left, right 이동 안 함
```python
# ❌ 찾은 후 이동 안 하면 무한 루프
if total == 0:
    result.append([nums[i], nums[left], nums[right]])
    # left, right 이동 안 함!

# ✅ 찾은 후 양쪽 이동
if total == 0:
    result.append([nums[i], nums[left], nums[right]])
    left += 1
    right -= 1
```

### 4. total < 0일 때 방향 혼동
```python
# ❌ 합이 작은데 right를 줄이면 더 작아짐
if total < 0:
    right -= 1  # 잘못!

# ✅ 합이 작으면 left를 늘려서 합을 키우기
if total < 0:
    left += 1   # 정렬된 배열에서 오른쪽이 더 큼
```

---

## 📊 복잡도 분석

### 왜 O(n²)인가?
```
for i in range(n):           # O(n)
    while left < right:      # O(n)
        ...

총: O(n) × O(n) = O(n²)
정렬: O(n log n)
최종: O(n²)  (n² > n log n)
```

### 브루트 포스와 비교

| 방법 | 시간 | 특징 |
|------|------|------|
| 브루트 포스 (3중 루프) | O(n³) | TLE |
| **정렬 + Two Pointer** | **O(n²)** | **최적** |

---

## 💪 핵심 포인트 체크리스트

- [ ] 왜 정렬이 필요한지 이해했나요?
- [ ] i를 고정하고 Two Pointer로 나머지를 찾는 방식을 이해했나요?
- [ ] 3가지 중복 제거 (i, left, right)를 모두 처리했나요?
- [ ] total < 0이면 left++, total > 0이면 right--인 이유를 아나요?

---

## 🎯 시간/공간 복잡도

- **시간: O(n²)** - 정렬 O(n log n) + 탐색 O(n²)
- **공간: O(1)** - 결과 제외 추가 공간 없음 (정렬 in-place)

---

## 🚀 관련 문제

- LeetCode 1: Two Sum (2개 합, HashMap)
- LeetCode 167: Two Sum II (정렬된 배열, Two Pointer)
- LeetCode 16: 3Sum Closest (합이 target에 가장 가까운)
- LeetCode 18: 4Sum (4개 합)

화이팅! 3Sum은 **정렬 + Two Pointer**의 대표 문제입니다! 🔥
